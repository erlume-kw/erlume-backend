/**
 * Income platform enum (dropdown options)
 */
export enum IncomePlatform {
	Online = "online",
	Instore = "in_store",
}

/**
 * Phase enum (e.g. Pre-launch, used in Expense, Income)
 */
export enum Phase {
	Prelaunch = "pre_launch",
	Launch = "launch",
	PostLaunch = "post_launch",
}

/**
 * Income type enum
 */
export enum IncomeType {
	Sale = "sale",
	Other = "other",
}
